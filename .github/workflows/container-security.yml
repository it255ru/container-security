    - name: Custom Dockerfile analysis
      id: analysis
      run: |
        echo "## 📋 Dockerfile Analysis Results" > analysis-results.md
        echo "" >> analysis-results.md
        echo "**Scan date:** $(date)" >> analysis-results.md
        echo "**Commit:** ${{ github.sha }}" >> analysis-results.md
        echo "**Branch:** ${{ github.ref }}" >> analysis-results.md
        echo "" >> analysis-results.md
        
        # Инициализация счетчиков
        ISSUE_COUNT=0
        CRITICAL_COUNT=0
        
        # Проверка конкретных проблем
        echo "### 🔍 Found Issues:" >> analysis-results.md
        
        # Проверка на ADD вместо COPY (игнорируем код возврата grep)
        if grep -q "ADD" Dockerfile; then
          echo "❌ **WARNING**: Используется ADD вместо COPY. COPY предпочтительнее для файлов" >> analysis-results.md
          ISSUE_COUNT=$((ISSUE_COUNT + 1))
        fi
        
        # Проверка на фиксированные версии
        if grep -q "node:14" Dockerfile; then
          echo "⚠️ **WARNING**: Используется фиксированная версия 'node:14'. Рекомендуется использовать конкретные версии" >> analysis-results.md
          ISSUE_COUNT=$((ISSUE_COUNT + 1))
        fi
        
        # Проверка на curl | bash
        if grep -q "curl.*|.*bash" Dockerfile; then
          echo "🚨 **CRITICAL**: Обнаружен опасный паттерн 'curl | bash'. Это угроза безопасности!" >> analysis-results.md
          ISSUE_COUNT=$((ISSUE_COUNT + 1))
          CRITICAL_COUNT=$((CRITICAL_COUNT + 1))
        fi
        
        # Проверка прав 777
        if grep -q "777" Dockerfile; then
          echo "🚨 **CRITICAL**: Используются права 777 - это серьезная уязвимость безопасности!" >> analysis-results.md
          ISSUE_COUNT=$((ISSUE_COUNT + 1))
          CRITICAL_COUNT=$((CRITICAL_COUNT + 1))
        fi
        
        # Проверка на pinning версий пакетов
        if { grep -q "npm install" Dockerfile && ! grep -q "package-lock.json\|pnpm-lock.yaml" Dockerfile; } then
          echo "⚠️ **WARNING**: Установка npm пакетов без фиксации версий" >> analysis-results.md
          ISSUE_COUNT=$((ISSUE_COUNT + 1))
        fi
        
        # Проверка на WORKDIR с абсолютными путями
        if ! grep -q "WORKDIR /app" Dockerfile; then
          echo "⚠️ **WARNING**: Рекомендуется использовать абсолютные пути в WORKDIR" >> analysis-results.md
          ISSUE_COUNT=$((ISSUE_COUNT + 1))
        fi
        
        # Анализ Hadolint результатов (с проверкой существования файла и обработкой ошибок)
        if [ -f hadolint-results.json ]; then
          echo "" >> analysis-results.md
          echo "### 🐳 Hadolint Results:" >> analysis-results.md
          
          # Безопасное чтение JSON файла
          if jq -e . hadolint-results.json > /dev/null 2>&1; then
            # Файл содержит валидный JSON
            HADOLINT_COUNT=$(jq '. | length' hadolint-results.json 2>/dev/null || echo "0")
            jq -r '.[] | "• **\(.level)**: Line \(.line): \(.message) (код: \(.code))"' hadolint-results.json 2>/dev/null >> analysis-results.md || echo "• Не удалось прочитать результаты Hadolint" >> analysis-results.md
            ISSUE_COUNT=$((ISSUE_COUNT + HADOLINT_COUNT))
          else
            echo "• Файл результатов Hadolint содержит невалидный JSON" >> analysis-results.md
          fi
        else
          echo "" >> analysis-results.md
          echo "### 🐳 Hadolint Results:" >> analysis-results.md
          echo "• Файл результатов не найден" >> analysis-results.md
        fi
        
        echo "" >> analysis-results.md
        echo "---" >> analysis-results.md
        echo "**Summary:** Найдено $ISSUE_COUNT проблем ($CRITICAL_COUNT критических)" >> analysis-results.md
        
        # Сохраняем счетчики в output (без использования GITHUB_OUTPUT если он недоступен)
        echo "issue_count=$ISSUE_COUNT" | tee -a $GITHUB_OUTPUT
        echo "critical_count=$CRITICAL_COUNT" | tee -a $GITHUB_OUTPUT
